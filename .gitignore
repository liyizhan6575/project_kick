*.deb
*.rpm
*.zip
*.tar.gz
# Data folders
data/
# Python/Conda stuff
__pycache__/
.ipynb_checkpoints/
.vscode/
# Editor / tooling config
.claude/
# Model artifacts (ignored everywhere EXCEPT committed reusable weights)
*.pkl
!football_metrics/model_utils/*.pkl
!football_metrics/model_utils/*.npy
# champion_model publishes ONLY the baked notebook — the pipeline modules, trained weights and the
# deployment snapshot are private; anything copied in to re-bake stays untracked
football_forecasts/champion_model/*
!football_forecasts/champion_model/champion_prediction.ipynb

# manim scene output (renders, caches) — never commit
football_visuals/animations/media/

# manim scene output (renders, caches) — never commit
media/
